#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: this program print text
 * parameter: their is no parameter
 *
 * Return: Always 0 (Success)
 */

	int main(void)
{
	int n;

	for (n = 0; n <= 9; n++)
	{
		printf("%d", n);
	}
	printf("\n");
	return (0);
}
